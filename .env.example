# HTTP port Railway routes traffic to. Railway injects PORT automatically.
PORT=8080

# Base domain for MagicDNS hostnames (must differ from the server's domain).
# Change this to something unique to you, e.g. ts.yourdomain.com
HEADSCALE_BASE_DOMAIN=example.com
